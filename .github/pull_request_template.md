## Summary

Describe what this PR changes and why.

## Checklist

- [ ] Focused change (no unrelated refactors)
- [ ] Typecheck/build passes (`npm --prefix web run build` and/or `npm --prefix server run build`)
- [ ] UI changes verified locally (screenshots optional)
- [ ] Docs updated if behavior changed

## Notes

Anything reviewers should know (tradeoffs, follow-ups, rollout/upgrade considerations).
